/**
 * Dashboard feature barrel export
 * Provides clean public API for dashboard functionality
 */

// Components
export * from './components';

// API
export * from './api';

// Types (re-export from shared)
export type { DailyEntry, HabitCompletion } from '../../types/data';